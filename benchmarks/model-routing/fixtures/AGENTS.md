# Fixture instructions

- Read `AGENTS.md`, `calc.go`, and `calc_test.go` before editing.
- Make the smallest correct change.
- Run tests after editing.
- Never claim tests passed unless the tool result says PASS.
